# Open. Everything here is meant to be found.
#
# This file is APPENDED to a Cloudflare managed block, above. Cloudflare's
# AI Crawl Control writes that block and disallows the AI training crawlers:
# GPTBot, ClaudeBot, CCBot, Google-Extended, Applebot-Extended, Bytespider,
# Amazonbot, meta-externalagent. That enforces the ai-train=no signal below
# rather than only declaring it.
#
# So do not add Allow groups here for any bot Cloudflare disallows. Two groups
# for the same user-agent merge into one with conflicting rules, and which rule
# wins is down to the individual crawler. Nothing in this file should argue with
# the block above it.
#
# Search and answer-time crawlers stay allowed, which is where the visibility
# is: Googlebot, Bingbot, PerplexityBot, and the user-initiated fetchers that
# OpenAI and Anthropic use when somebody actually asks about Yair.
#
# The Content-Signal line states what AI may do with this content, per
# contentsignals.org.
#   search    yes. Appear in search results.
#   ai-input  yes. Be read at answer time, so assistants quote the real
#             words rather than guessing. This is where the benefit is.
#   ai-train  no. Not for use as model training data.
#
# The signal is repeated in every group on purpose. A crawler that matches a
# named group ignores the * group completely, so a signal written only under *
# would never reach the named bots.
#
# ai-train is off deliberately. Permitting it would gain a site this size
# essentially nothing, and it is the only one of the three that cannot be
# taken back later: a signal can be changed, but text already absorbed into
# a trained model stays absorbed. The essays are coming, and this keeps that
# decision open. Visibility is unaffected, since search and ai-input carry
# all of it.

User-agent: *
Allow: /
Content-Signal: search=yes, ai-input=yes, ai-train=no

User-agent: Googlebot
Allow: /
Content-Signal: search=yes, ai-input=yes, ai-train=no

User-agent: Bingbot
Allow: /
Content-Signal: search=yes, ai-input=yes, ai-train=no

User-agent: PerplexityBot
Allow: /
Content-Signal: search=yes, ai-input=yes, ai-train=no

Sitemap: https://yairwalton.com/sitemap.xml
